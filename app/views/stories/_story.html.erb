<li id="<%= "#{dom_id(story)}_container" %>" class="py-2 px-4 border-b border-gray-300">
  <%= turbo_frame_tag dom_id(story) do  %>
    <%= link_to story.description, edit_story_path(story) %>
  <% end %>
</li>