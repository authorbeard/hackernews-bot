<div class="mx-auto">
  <h2 class="mb-2 text-2xl text-gray-900 flex justify-center">
    <% if @stories.empty? %> 
      No stories yet. Check back in an hour or reach out to your friendly local engineer to see which computer needs to be kicked. 
    <% else %>
      Top 10 HackerNews Stories as of <%= @last_updated.localtime.strftime("%A,%e %B %Y, %I %p %Z") if @last_updated %>
    <% end %>
  </h2>

  <% if @stories.presence %>
    <div class="w-full bg-gray-100 py-4 px-4 border border-gray-200 rounded shadow-sm">
      <ul id="stories">
        <%= render @stories %>
      </ul>
    </div>
    <% end %>
</div>


